X <- as.matrix(read.table('uspsdata.txt'))
y <- scan('uspscl.txt')

length(X[,1])

# reorder matrix columns based on 2nd row
test[,order(test[2,])]


k=k[order(as.numeric(row.names(k))),]


y_prime[as.numeric(tr_data$row)] = y_prime[as.numeric(tr_data$row)] + tr_Y


total=0
for(i in nrow(m)) {
	total=total + w[m[i]]*y[m[i]]*
}

for(i in nrow(m)) m[i]=y[m[1]]